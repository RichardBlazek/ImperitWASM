@code{
	SoldierItem? item = null;
	public void Open(SoldierItem @new)
	{
		item = @new;
		StateHasChanged();
	}
}

<PopupBox Show="item is not null" OnClose="() => item = null">
	@item?.N (@item?.S) &ndash; @(new MarkupString(item?.T ?? ""))
</PopupBox>